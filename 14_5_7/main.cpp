#include <cstdio>

int m, n, k, l, d, a[1000], b[1000];

int main()
{
    scanf("%d%d%d%d%d", &m, &n, &k, &l, &d);
    for (int i = 0; i < d; i++)
    {
        scanf("%d%d%d%d", )
    }
    return 0;
}